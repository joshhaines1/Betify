To Do:

leaderboards

Void bets / PUSH


After you create an event, it doesnt show up in the list
    - decide whether to add it manually or trigger a refresh

Trigger refresh after locking or updating event. After locking event and then going to home and then back to group it appears unlocked cuz its grabbing from stale cache

When locking an event, only refetch that event by ID??? So delete the event and refetch the event by id and add it to list? Or just lock it and refresh the whole list?

Whne event is locked due to date, have the user lock it lazily